language: ruby
sudo: false
rvm:
  - 2.0.0
  - 2.1.10
  - 2.2.8
  - 2.3.5
  - 2.4.2

cache:
  - bundler

before_install:
  - gem install bundler # To use 1.8.x or later

before_script:
  - bundle exec rake update_assets
